{
  "name": "schnitt-und-stil",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port=3000 --host=0.0.0.0",
    "build": "mkdir -p dist && cp index.html dist/ && (cp -r img dist/ 2>/dev/null || true)",
    "preview": "vite preview",
    "clean": "rm -rf dist",
    "lint": "echo 'No TS linting required for static build'"
  },
  "dependencies": {
    "vite": "^5.0.0"
  }
}
